## ADDED Requirements

### Requirement: Explore gap card uses purple color theme
The explore gap card (304×176 rx=12) SHALL have a purple header (fill=#ede9fe) with title "差了什么 · 待探索" (11px/700/#7c3aed) and subtitle showing item count (9px/#94a3b8) (SVG lines 150-154).

### Requirement: Explore items are plain text rows without border cards
Each explore item SHALL render as a plain text row: title (11px/600/#0f172a) followed by associated trunk text (9px/#94a3b8) in the format "← 关联: {prerequisite_title} ✓" (SVG lines 157-180).

### Requirement: Agent link bar at bottom
A link bar (284×16 rx=4 fill=#ede9fe) SHALL display "→ 和智能体对话，走一条推荐路径" (8px/600/#7c3aed) at the bottom of the section (SVG lines 182-185).
