# 软件杯A3赛题 - 智能体与工具输入输出收口设计（最终精简版）

## 一、架构设计准则

- 中心化调度多智能体协同架构
- 单一职责
- 工具最小权限
- 严格输入输出收口
- 无横向互调
- 无自主调度
- 中心统一驱动
- MCP服务松耦合协同

## 二、全局调度核心智能体

### 总调度决策Agent

**定位**：全局唯一调度中枢，无业务生成能力。

**工具1：MCP子服务调度工具**
- 输入：用户请求、系统事件、任务参数
- 输出：子智能体任务指令、服务启停信号、任务流转状态

**工具2：多智能体结果合并规整工具**
- 输入：各子智能体原始输出数据
- 输出：统一格式的结构化前端可渲染数据

**核心职责**：任务拆分、流程排序、异常重试、结果汇总、全局管控。

## 三、必备业务智能体（赛题硬性要求）

### 1. 学习画像构建Agent

**定位**：学生多维身份属性构建与动态更新。

**工具1：自然语言对话特征抽取工具**
- 输入：用户对话文本、专业信息、学习目标
- 输出：学生基础特征标签集合

**工具2：学习行为标签化工具**
- 输入：学生学习记录、答题数据、资源使用行为
- 输出：六维及以上学生身份属性结构化画像

**核心职责**：画像初始化、画像实时更新、标准化数据输出。

**输入收口**：
- 首次进入：用户对话文本、学生基本信息、课程/专业信息、学习目标
- 增量更新：新对话、新答题结果、新资源使用记录、新学习事件

**输出收口**：
- 统一输出学生画像 JSON
- 仅输出结构化结果，不直接生成教学内容
- 不跨越到路径规划、资源生成、答疑生成等下游任务

**画像标准字段**：
- 基础信息：`student_id`、`grade`、`major`、`course_name`
- 目标信息：`learning_goal`、`target_level`、`deadline`
- 能力标签：`knowledge_mastery`、`concept_gaps`、`practice_ability`、`comprehension_level`
- 行为标签：`study_frequency`、`study_duration`、`resource_preference`、`answer_pattern`
- 风格标签：`learning_style`、`attention_pattern`、`difficulty_tolerance`
- 风险标签：`bottleneck_topics`、`dropout_risk`、`recent_anomaly`

**处理流程**：
1. 接收用户对话与学习数据
2. 抽取显式意图与隐式特征
3. 合并历史画像与当前增量数据
4. 计算六维以上画像标签
5. 生成可供下游 Agent 直接消费的标准化画像

**标准输出示例**：
```json
{
	"student_id": "U10001",
	"learning_goal": "掌握Python基础语法",
	"target_level": "入门",
	"knowledge_mastery": {
		"variables": 0.6,
		"loops": 0.3,
		"functions": 0.2
	},
	"concept_gaps": ["函数参数", "循环嵌套"],
	"study_frequency": "low",
	"study_duration": "short",
	"resource_preference": ["图文", "示例代码"],
	"learning_style": "example-driven",
	"difficulty_tolerance": "medium",
	"dropout_risk": "medium"
}
```

### 2. 个性化学习路径规划Agent

**定位**：个性化动态学习路径生成。

**工具1：课程知识点图谱查询工具**
- 输入：课程名称、知识点关键词
- 输出：知识点层级结构、依赖关系数据

**工具2：学习路径推演工具**
- 输入：结构化学生画像、知识点图谱、学习目标
- 输出：分阶段优先级学习路径清单

**核心职责**：路径生成、路径动态调整、学习顺序规划。

### 3. 统一个性化资源生成Agent

**定位**：全品类静态教学资源统一生成。

**工具1：结构化文档生成工具**
- 输入：知识点名称、学生能力层级、路径节点信息
- 输出：知识点讲解文档、拓展阅读材料

**工具2：思维导图生成工具**
- 输入：知识点清单、知识层级数据
- 输出：结构化思维导图文件

**工具3：分层题库生成工具**
- 输入：薄弱知识点、学生能力层级、题型难度参数
- 输出：含题干答案解析的标准化题库

**工具4：代码实操案例生成工具**
- 输入：编程知识点、课程实操规范
- 输出：带注释可运行代码、实操步骤文档

**核心职责**：多类型个性化静态教学资源输出。

### 4. 内容安全与防幻觉审核Agent

**定位**：内容事实校验与安全过滤。

**工具1：学术内容事实校验工具**
- 输入：各智能体原始生成资源数据
- 输出：无幻觉、事实准确的内容数据

**工具2：敏感内容过滤工具**
- 输入：待审核文本及多媒体描述数据
- 输出：脱敏合规安全内容数据

**核心职责**：内容纠错、内容合规过滤。

## 四、加分拓展智能体

### 1. 智能答疑辅导Agent

**定位**：学生学习问题多模态答疑。

**工具1：知识库优化RAG答疑生成工具**
- 输入：学生提问文本、对话上下文、课程知识库索引
- 输出：知识库精准答疑文本、图解答疑素材

**核心职责**：基于RAG检索知识库精准答疑，答疑内容本地化精准优化。

### 2. 学习效果评估Agent

**定位**：学习效果量化与学习方案优化。

**工具1：学习行为多维度分析工具**
- 输入：学习时长数据、答题数据、资源使用数据
- 输出：多维度学习效果量化指标

**工具2：学习方案动态调整工具**
- 输入：学习效果量化指标、现有学习方案数据
- 输出：学习路径优化策略、资源推送调整策略

**核心职责**：学习效果评估、学习方案动态优化。

> 注：文档部分内容可能由AI生成。
